{
  "name": "nexura",
  "type": "module",
  "engines": { "node": ">=16.0.0", "npm": ">=7.0.0" },
  "workspaces": ["src/nexura_frontend"],
  "scripts": {
    "dev": "npm run dev -w src/nexura_frontend",
    "start": "npm run start -w src/nexura_frontend",
    "build": "npm run build -w src/nexura_frontend",
    "prebuild": "npm run prebuild -w src/nexura_frontend",
    "test": "npm run test -w src/nexura_frontend || true"
  }
}
